/**
 * 휴식 시간 증감 단위 (5초)
 */
export const REST_STEP_MS = 5_000;
/**
 * 휴식 시간 최소값 (0초 → 휴식 사용 안 함)
 */
export const REST_MIN_MS = 0;
/**
 * 휴식 시간 최대값 (90초)
 */
export const REST_MAX_MS = 90_000;
/**
 * 기본 휴식 시간 (15초)
 */
export const DEFAULT_REST_DURATION_MS = 15_000;

/**
 * 운동 시간 증감 단위 (5초)
 */
export const WORKOUT_STEP_MS = 5_000;
/**
 * 운동 시간 최소값 (5초)
 */
export const WORKOUT_MIN_MS = 5_000;
/**
 * 운동 시간 최대값 (10분)
 */
export const WORKOUT_MAX_MS = 600_000;
/**
 * 기본 운동 시간 (30초)
 */
export const DEFAULT_WORKOUT_DURATION_MS = 30_000;